[build]
builder = "nixpacks"

[build.nixpacks]
plan = """
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps"]

[start]
cmd = "python main.py"
